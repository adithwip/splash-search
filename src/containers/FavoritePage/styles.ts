import { createStyles } from '@mantine/core';

const useStyles = createStyles((theme) => ({
  image_wrapper: {
    position: "relative",
    minWidth: "33%",
    height: 200,
  }
}))

export default useStyles